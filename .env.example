# NetAlign - Environment Variables Configuration Template
# Copy this file to a new file named ".env" in the same directory and modify the values.

# The port that the Hono backend server will listen on
# Fallback default: 5000 (required for default Vite dev proxy)
PORT=5000

# Environment mode (development, production, test)
# production → auth on by default unless NETALIGN_AUTH_MODE is set
NODE_ENV=development

# SQLite database file path (optional)
# Default: server/data/netalign.db
# NETALIGN_DB_PATH=server/data/netalign.db

# Directory for `bun run db:backup` timestamped files (optional)
# Default: server/data/backups
# NETALIGN_BACKUP_DIR=server/data/backups

# Skip auto-import of JSON seed files on first startup (optional, for tests)
# NETALIGN_SKIP_JSON_MIGRATE=1

# Authentication mode: on | off | true | false | 1 | 0
# Unset: on when NODE_ENV=production, off otherwise (local dev / unit tests).
# When on: topology routes require a session; list/mutate scoped to owner.
# See docs/auth.md
# NETALIGN_AUTH_MODE=on

# Swagger UI at GET /api/docs (CDN assets)
# Unset: enabled when not production. Force with 1/true; disable with 0/false.
# NETALIGN_OPENAPI_UI=1

# Comma-separated topology ids that cannot be deleted via the API (seed safety).
# Default when unset: topology-1
# PROTECTED_TOPOLOGY_IDS=topology-1

# Comma-separated browser origins allowed to call the API (CORS).
# Development default (when unset): http://localhost:3000 and common Vite ports.
# Production (when unset): deny cross-origin browser access — set your frontend URL(s).
# Prefer explicit origins (not *) when using session cookies (credentials: include).
# CORS_ORIGINS=https://app.example.com,https://www.example.com

# Structured logging (JSON lines to stdout/stderr)
# LOG_LEVEL=info
# LOG_REQUESTS=1

# --- Frontend (Vite) ---
# Backend origin embedded at build time for split deployments.
# Leave empty or omit for same-origin / Vite dev proxy (`/api` → localhost:5000).
# VITE_API_BASE=https://api.example.com
